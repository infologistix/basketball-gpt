[
  {
    "question": "How many columns are in table silver.s_el_ballbesitze?",
    "sql": "SELECT table_schema, table_name, count(*)::integer AS column_count\nFROM information_schema.columns\nWHERE table_schema = 'silver' AND table_name = 's_el_ballbesitze'\nGROUP BY table_schema, table_name",
    "tables": [
      "silver.s_el_ballbesitze"
    ],
    "tags": [
      "schema",
      "columns",
      "metadata"
    ],
    "doc": "Use information_schema.columns for exact column counts. Do not list all tables when the user names one table."
  },
  {
    "question": "Show columns in table bronze.b_el_boxscore",
    "sql": "SELECT table_schema, table_name, column_name, data_type, ordinal_position\nFROM information_schema.columns\nWHERE table_schema = 'bronze' AND table_name = 'b_el_boxscore'\nORDER BY ordinal_position",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "schema",
      "columns",
      "metadata"
    ],
    "doc": "For column-list questions, return column metadata ordered by ordinal_position."
  },
  {
    "question": "Show 20 sample rows from bronze.b_el_boxscore",
    "sql": "SELECT *\nFROM bronze.b_el_boxscore\nLIMIT 20",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "sample",
      "rows",
      "preview"
    ],
    "doc": "For sample rows, return raw rows with a small LIMIT. Do not aggregate."
  },
  {
    "question": "Draw a bar chart of total pts by player_name from bronze.b_el_boxscore, top 20, sort descending",
    "sql": "SELECT player_name, SUM(pts) AS total_pts\nFROM bronze.b_el_boxscore\nWHERE pts IS NOT NULL\nGROUP BY player_name\nHAVING SUM(pts) > 0\nORDER BY total_pts DESC\nLIMIT 20",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "draw",
      "chart",
      "bar",
      "euroleague",
      "points",
      "player"
    ],
    "doc": "For top scoring charts, group by player_name, sum pts, remove zero totals, order by total_pts, and limit results."
  },
  {
    "question": "Draw a bar chart of top EuroCup players by total points",
    "sql": "SELECT player_name, SUM(pts) AS total_pts\nFROM bronze.b_ec_boxscore\nWHERE pts IS NOT NULL\nGROUP BY player_name\nHAVING SUM(pts) > 0\nORDER BY total_pts DESC\nLIMIT 20",
    "tables": [
      "bronze.b_ec_boxscore"
    ],
    "tags": [
      "draw",
      "chart",
      "bar",
      "eurocup",
      "points",
      "player"
    ],
    "doc": "EuroCup bronze tables use the b_ec prefix."
  },
  {
    "question": "Draw a scatter plot of pts vs minutes from bronze.b_bbl_boxscore, using only rows where minutes is not null",
    "sql": "SELECT split_part(minutes, ':', 1)::numeric + split_part(minutes, ':', 2)::numeric / 60.0 AS minutes,\n       pts,\n       player_name,\n       player_team\nFROM bronze.b_bbl_boxscore\nWHERE minutes ~ '^[0-9]{1,3}:[0-9]{2}$'\n  AND minutes <> '00:00'\n  AND pts IS NOT NULL\nLIMIT 500",
    "tables": [
      "bronze.b_bbl_boxscore"
    ],
    "tags": [
      "draw",
      "scatter",
      "minutes",
      "points",
      "bbl"
    ],
    "doc": "Boxscore minutes are text in MM:SS format. Convert them to decimal minutes before charting."
  },
  {
    "question": "Compare EuroLeague player scoring efficiency from silver.s_el_player_stats_prospiel and gold.g_el_players",
    "sql": "SELECT s.player_name,\n       s.player_team,\n       COUNT(*) AS games_played,\n       SUM(s.pts) AS total_pts,\n       ROUND(AVG(s.pts)::numeric, 2) AS avg_pts_per_game\nFROM silver.s_el_player_stats_prospiel AS s\nGROUP BY s.player_name, s.player_team\nHAVING COUNT(*) >= 5\nORDER BY total_pts DESC\nLIMIT 25",
    "tables": [
      "silver.s_el_player_stats_prospiel",
      "gold.g_el_players"
    ],
    "tags": [
      "silver",
      "gold",
      "euroleague",
      "efficiency",
      "points",
      "player"
    ],
    "doc": "When a join key is uncertain, prefer a reliable silver aggregation over inventing a fragile join. Use gold only when column names clearly match."
  },
  {
    "question": "How many rows are in each bronze table?",
    "sql": "SELECT 'b_bbl_boxscore' AS table_name, COUNT(*) AS row_count FROM bronze.b_bbl_boxscore\nUNION ALL SELECT 'b_bbl_playbyplay', COUNT(*) FROM bronze.b_bbl_playbyplay\nUNION ALL SELECT 'b_bbl_player_info', COUNT(*) FROM bronze.b_bbl_player_info\nUNION ALL SELECT 'b_bbl_team_image', COUNT(*) FROM bronze.b_bbl_team_image\nUNION ALL SELECT 'b_bbl_wurfposition', COUNT(*) FROM bronze.b_bbl_wurfposition\nUNION ALL SELECT 'b_cl_boxscore', COUNT(*) FROM bronze.b_cl_boxscore\nUNION ALL SELECT 'b_cl_hist_links', COUNT(*) FROM bronze.b_cl_hist_links\nUNION ALL SELECT 'b_cl_playbyplay', COUNT(*) FROM bronze.b_cl_playbyplay\nUNION ALL SELECT 'b_cl_player_info', COUNT(*) FROM bronze.b_cl_player_info\nUNION ALL SELECT 'b_cl_player_info_alt', COUNT(*) FROM bronze.b_cl_player_info_alt\nUNION ALL SELECT 'b_cl_player_info_test', COUNT(*) FROM bronze.b_cl_player_info_test\nUNION ALL SELECT 'b_ec_boxscore', COUNT(*) FROM bronze.b_ec_boxscore\nUNION ALL SELECT 'b_ec_playbyplay', COUNT(*) FROM bronze.b_ec_playbyplay\nUNION ALL SELECT 'b_ec_player_info', COUNT(*) FROM bronze.b_ec_player_info\nUNION ALL SELECT 'b_el_boxscore', COUNT(*) FROM bronze.b_el_boxscore\nUNION ALL SELECT 'b_el_playbyplay', COUNT(*) FROM bronze.b_el_playbyplay\nUNION ALL SELECT 'b_el_player_info', COUNT(*) FROM bronze.b_el_player_info\nUNION ALL SELECT 'b_el_wurfposition', COUNT(*) FROM bronze.b_el_wurfposition\nORDER BY table_name",
    "tables": [
      "bronze"
    ],
    "tags": [
      "row",
      "count",
      "metadata",
      "bronze",
      "compare",
      "euroleague",
      "eurocup",
      "bbl",
      "champions league"
    ],
    "doc": "Table names cannot be parameterized as SQL values, so a single grouped query can't compute per-table row counts. The fix is a UNION ALL with one SELECT 'literal_table_name', COUNT(*) FROM schema.table_name per table, listing each table explicitly by name - this is real, executable SQL, not a limitation to explain away. Use this same technique for 'compare row counts across EuroLeague/EuroCup/BBL/Champions League' style questions too: filter the UNION ALL to just the b_el_/b_ec_/b_bbl_/b_cl_ tables the question asks about, verified working against the live database with real row counts."
  },
  {
    "question": "Which players have the most rows in bronze.b_el_boxscore?",
    "sql": "SELECT player_name, COUNT(*) AS row_count\nFROM bronze.b_el_boxscore\nGROUP BY player_name\nORDER BY row_count DESC\nLIMIT 25",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "count",
      "player",
      "euroleague",
      "rows"
    ],
    "doc": "Use COUNT(*) for row-frequency questions, not SUM(pts)."
  },
  {
    "question": "Who made the most three-pointers in EuroLeague season 2025-2026?",
    "sql": "SELECT player_name, SUM(three_p) AS threes_made\nFROM bronze.b_el_boxscore\nWHERE date >= '2025-08-01' AND date < '2026-08-01'\nGROUP BY player_name\nORDER BY threes_made DESC\nLIMIT 10",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "season",
      "2025-2026",
      "three_p",
      "date",
      "euroleague",
      "eurocup"
    ],
    "doc": "bronze tables have no season column, only date - a query for a named season MUST filter date to that season's range (August of the first year through July of the next, since the competition year crosses the calendar year boundary) or it silently mixes every season together. GROUP BY player_name alone, not (player_name, player_team): mid-season sponsor renames give the same player two team-name rows in one season."
  },
  {
    "question": "Which EuroCup team scored the most total points this season?",
    "sql": "WITH normalized AS (\n  SELECT CASE home_team\n           WHEN 'HAPOEL BANK YAHAV JERUSALEM' THEN 'HAPOEL MIDTOWN JERUSALEM'\n           WHEN 'PANIONIOS ATHENS' THEN 'PANIONIOS COSMORAMA TRAVEL ATHENS'\n           ELSE home_team\n         END AS team,\n         link, MAX(home_team_total_point) AS pts\n  FROM bronze.b_ec_boxscore\n  WHERE date >= '2025-08-01' AND date < '2026-08-01'\n  GROUP BY home_team, link\n  UNION ALL\n  SELECT CASE away_team\n           WHEN 'HAPOEL BANK YAHAV JERUSALEM' THEN 'HAPOEL MIDTOWN JERUSALEM'\n           WHEN 'PANIONIOS ATHENS' THEN 'PANIONIOS COSMORAMA TRAVEL ATHENS'\n           ELSE away_team\n         END AS team,\n         link, MAX(away_team_total_point) AS pts\n  FROM bronze.b_ec_boxscore\n  WHERE date >= '2025-08-01' AND date < '2026-08-01'\n  GROUP BY away_team, link\n)\nSELECT team, SUM(pts) AS total_pts\nFROM normalized\nGROUP BY team\nORDER BY total_pts DESC\nLIMIT 10",
    "tables": [
      "bronze.b_ec_boxscore"
    ],
    "tags": [
      "team",
      "points",
      "total",
      "rank",
      "top",
      "eurocup",
      "euroleague",
      "rename",
      "sponsor"
    ],
    "doc": "TWO separate required steps, both load-bearing - dropping either one silently corrupts the answer: (1) home_team_total_point/away_team_total_point repeat on every player row of a game (~23-24 rows per team per game, all identical) - you MUST collapse to one row per team-game first via GROUP BY team, link and MAX(...), never SUM the raw per-player rows directly, or the total overcounts by roughly the roster size (verified: 2073 correct vs 48556 when this step is dropped, a 23x error). (2) Team-level aggregation must ALSO normalize mid-season sponsor-renamed team names to one canonical name via CASE WHEN before grouping, or a renamed team's points silently split across two rows and it drops out of the ranking entirely (verified: HAPOEL MIDTOWN JERUSALEM never appears in the EuroCup top 10 without this; correctly ranks 6th at 1810 combined points with it). Both steps are independent of each other - do not skip the MAX(...) dedup just because a question does not mention renamed teams."
  },
  {
    "question": "Which BBL team scored the most total points in the 2024-25 season?",
    "sql": "SELECT team, SUM(pts) AS total_pts, COUNT(*) AS games\nFROM (\n  SELECT home_team AS team, home_team_total_point AS pts FROM silver.s_bbl_ptstotal WHERE date >= '2024-08-01' AND date < '2025-08-01'\n  UNION ALL\n  SELECT away_team AS team, away_team_total_point AS pts FROM silver.s_bbl_ptstotal WHERE date >= '2024-08-01' AND date < '2025-08-01'\n) sub\nGROUP BY team\nHAVING COUNT(*) > 2\nORDER BY total_pts DESC\nLIMIT 10",
    "tables": [
      "silver.s_bbl_ptstotal"
    ],
    "tags": [
      "team",
      "points",
      "total",
      "rank",
      "top",
      "bottom",
      "average",
      "bbl",
      "bundesliga",
      "cup",
      "phantom"
    ],
    "doc": "BBL team-level aggregation must exclude ProA cup opponents with HAVING COUNT(*) > 2 on the team-game grouping, or 6-7 phantom cup-opponent teams (each with only 1-2 games all season) get counted alongside the ~17-18 real Bundesliga teams - verified against live data: 24 raw team names drop to 17 real ones after filtering. Never changes who ranks #1 (phantom teams have tiny totals from so few games) but corrupts team counts, bottom-N rankings, and any per-team average."
  },
  {
    "question": "What is the average three-point shooting percentage for players in the Champions League 2024-25 season, minimum 50 attempts?",
    "sql": "WITH qualifying AS (\n  SELECT player_name, SUM(three_pa) AS attempts, SUM(three_p) AS made\n  FROM bronze.b_cl_boxscore\n  WHERE date >= '2024-08-01' AND date < '2025-08-01'\n  GROUP BY player_name\n  HAVING SUM(three_pa) >= 50\n)\nSELECT SUM(made) * 100.0 / SUM(attempts) AS avg_three_p_pct, COUNT(*) AS qualifying_players\nFROM qualifying",
    "tables": [
      "bronze.b_cl_boxscore"
    ],
    "tags": [
      "average",
      "mean",
      "percentage",
      "three point",
      "shooting",
      "minimum",
      "attempts",
      "champions league"
    ],
    "doc": "An 'average X, minimum N attempts' question needs a TWO-LEVEL aggregate, not a leaderboard: first collapse to one row per player-season with GROUP BY player_name and HAVING SUM(attempts) >= N (N is a season total, never a per-row WHERE filter - a single game essentially never has 50 three-point attempts, so WHERE three_pa >= 50 silently returns zero rows), then aggregate that qualifying set into the final single number. Do not answer with ORDER BY pct DESC LIMIT 20 and let the summary step average the returned rows - verified against live data: the true average across all 47 qualifying players is 37.4%, but averaging only the top 20 by percentage gives a biased 43.6%."
  },
  {
    "question": "What was Real Madrid's win-loss record in the EuroLeague 2025-26 season?",
    "sql": "WITH deduped AS (\n  SELECT link, home_team, away_team, MAX(home_team_total_point) AS home_pts, MAX(away_team_total_point) AS away_pts\n  FROM bronze.b_el_boxscore\n  WHERE date >= '2025-08-01' AND date < '2026-08-01'\n  GROUP BY link, home_team, away_team\n),\nreoriented AS (\n  SELECT home_team AS team, home_pts AS team_pts, away_pts AS opp_pts FROM deduped\n  UNION ALL\n  SELECT away_team AS team, away_pts AS team_pts, home_pts AS opp_pts FROM deduped\n)\nSELECT\n  COUNT(*) AS games_played,\n  SUM(CASE WHEN team_pts > opp_pts THEN 1 ELSE 0 END) AS wins,\n  SUM(CASE WHEN team_pts < opp_pts THEN 1 ELSE 0 END) AS losses\nFROM reoriented\nWHERE team ILIKE '%REAL MADRID%'",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "record",
      "win",
      "wins",
      "loss",
      "losses",
      "win-loss",
      "w-l",
      "season"
    ],
    "doc": "A team's win-loss record needs the UNION ALL reorientation pattern (one row per team-game, deduplicated first per the boxscore-duplication note) with BOTH branches relabeled to team_pts/opp_pts before comparing - never keep raw home_pts/away_pts columns after the UNION and compare those directly, and never write a CASE that only checks the home-team win condition. Both mistakes silently drop every away game for the target team. Verified against live data: a query checking only 'home_team ILIKE team AND home_pts > away_pts' reported Real Madrid 35-9 for the full season; the real record, from this correctly reoriented query, is 28-16 - a materially different, wrong record, not just an imprecise one. This mistake recurs often even when the lesson has been taught before - double-check every win-loss query against this exact pattern rather than improvising a new one."
  },
  {
    "question": "Aus welchen Ländern kamen die meisten Punkte in der Champions-League-Saison 2025–2026?",
    "sql": "WITH info AS (\n  SELECT DISTINCT ON (btrim(player_name))\n         btrim(player_name) AS player_name, nationality\n  FROM bronze.b_cl_player_info\n),\nsaison AS (\n  SELECT btrim(player_name) AS player_name, SUM(pts) AS punkte\n  FROM bronze.b_cl_boxscore\n  WHERE date >= '2025-08-01' AND date < '2026-08-01'\n  GROUP BY 1\n)\nSELECT i.nationality, COUNT(*) AS spieler, SUM(s.punkte) AS punkte\nFROM saison s\nJOIN info i USING (player_name)\nGROUP BY 1\nORDER BY punkte DESC",
    "tables": [
      "bronze.b_cl_boxscore",
      "bronze.b_cl_player_info"
    ],
    "tags": [
      "nationality",
      "country",
      "land",
      "länder",
      "player_info",
      "join",
      "distinct on",
      "duplicate",
      "champions league",
      "points",
      "punkte",
      "season"
    ],
    "doc": "Joining a boxscore to a *_player_info table needs DISTINCT ON. player_info holds ONE ROW PER CRAWLED SEASON per player - 206 of 1165 Champions League players have 2-4 rows - so a plain JOIN fans out and multiplies their points by that row count. Verified against live data: without DISTINCT ON, USA came out at 17752 points instead of 9813, and the inflation is NOT uniform, so the ranking itself changes (Turkey rose from 6th to 3rd, Germany fell from 5th to 7th). Also note: player_info has NO season column, so the season must be filtered on the boxscore side and only then joined for the attribute - filtering player_info alone silently mixes every crawled season. Use btrim() on both sides of the name join; without it players with stray whitespace drop out entirely. Champions League has three profile tables (b_cl_player_info, _alt, _test) - b_cl_player_info is the canonical one."
  },
  {
    "question": "Wer war in der BBL-Saison 2025–2026 der beste Rebounder jedes Teams (mindestens 15 Spiele)?",
    "sql": "WITH je_spieler AS (\n  SELECT player_team AS team,\n         btrim(player_name) AS spieler,\n         COUNT(*) AS spiele,\n         ROUND(AVG(reb), 2) AS reb_schnitt\n  FROM bronze.b_bbl_boxscore\n  WHERE date_final >= '2025-08-01' AND date_final < '2026-08-01'\n  GROUP BY 1, 2\n  HAVING COUNT(*) >= 15\n),\nrang AS (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY team ORDER BY reb_schnitt DESC) AS rn\n  FROM je_spieler\n)\nSELECT team, spieler, spiele, reb_schnitt\nFROM rang\nWHERE rn = 1\nORDER BY reb_schnitt DESC",
    "tables": [
      "bronze.b_bbl_boxscore"
    ],
    "tags": [
      "rebounds",
      "rebounder",
      "best per team",
      "per group",
      "row_number",
      "partition by",
      "window function",
      "rank",
      "bbl",
      "bundesliga",
      "minimum",
      "games"
    ],
    "doc": "A 'best X per team/group' question needs ROW_NUMBER() OVER (PARTITION BY group ORDER BY metric DESC) in a CTE, then WHERE rn = 1 - not a plain GROUP BY, which cannot return the row that produced the maximum. Two traps specific to this database. First, do NOT source this from gold.g_player_profile_*: its pts/reb/trb columns are ALREADY per-game averages, so dividing them by gp again yields rebounds-per-game-per-game, which systematically favours players with few games - verified against live data, that mistake picked the wrong player for 8 of 18 BBL teams. Second, g_player_profile_bbl is incomplete (265 players), so several teams' actual best rebounder does not exist in it at all; any question about coverage across all teams or all players must come from the bronze boxscore. Use player_team for the player's own team - home_team_final/away_team_final are the fixture, not the player's team."
  },
  {
    "question": "Welche Spieler in der Bundesliga kommen aus Deutschland?",
    "sql": "SELECT DISTINCT ON (btrim(player_name))\n       btrim(player_name) AS spieler, player_team, nationality\nFROM bronze.b_bbl_player_info\nWHERE nationality ILIKE '%Deutschland%'\nORDER BY btrim(player_name)",
    "tables": [
      "bronze.b_bbl_player_info"
    ],
    "tags": [
      "nationality",
      "country",
      "nationalität",
      "land",
      "deutschland",
      "germany",
      "bbl",
      "bundesliga",
      "player_info",
      "distinct on",
      "ilike"
    ],
    "doc": "Country filters must use the spelling of the league's own table, and always ILIKE, never '='. b_bbl_player_info stores nationality in GERMAN with a 2-letter ISO code in brackets, so ILIKE '%Germany%' matches 0 of 859 rows while ILIKE '%Deutschland%' matches 392 - verified against live data. The bracket is not reliable either (260 rows 'Deutschland (DE)', 70 rows plain 'Deutschland'), and dual nationals hold both countries in one cell separated by a newline (86 rows), so a substring match is the only form that catches all three shapes. DISTINCT ON is required because player_info holds one row per crawled season: 392 rows correspond to 304 distinct players. The encoding differs per league - EuroLeague and EuroCup use English full names ('Germany', and EC writes 'Turkiye'), Champions League uses 3-letter codes ('GER') with comma-separated duals - so this literal must be changed per league and a single cross-league country query is not possible."
  }
]
