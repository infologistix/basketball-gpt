__pycache__/
*.py[cod]
.env
nba.db
# Keep secrets out but track the theme: config.toml is what gives the app a
# deliberate palette instead of Streamlit's stock dark defaults.
.streamlit/*
!.streamlit/config.toml
.venv/
venv/
.DS_Store
.docker-build-config/
docs/

# Database dumps: ~26 MB each, would need LFS. Already excluded from the build
# context by .dockerignore; keep them out of git too.
sql_dumps/*.gz
